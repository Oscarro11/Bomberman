#pragma once

#include <filesystem>

namespace AssetPaths {

    inline const std::filesystem::path ROOT =
        "../assets";

    inline const std::filesystem::path MAPS =
        ROOT / "mapas";
}